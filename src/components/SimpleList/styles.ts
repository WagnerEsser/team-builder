import styled from "styled-components";
import { containerBorderColor } from "../../colors";
import { FlexBox } from "../../screens/Home/styles";

export const WrapperArrow = styled(FlexBox)`
    border: 1px solid ${containerBorderColor};
    border-radius: 100%;
`
